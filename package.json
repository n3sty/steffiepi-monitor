{
  "name": "steffiepi-monitor",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=web",
    "dev:monitor": "turbo dev --filter=monitor",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.5.6",
    "@types/node": "^20",
    "typescript": "^5"
  },
  "packageManager": "npm@10.0.0"
}
